overwrite: true
schema: "https://apps.stardog.com/api/graphql"
documents: "src/portal/**/*.graphql"
generates:
  src/portal/sdk.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
    config:
      documentMode: string
