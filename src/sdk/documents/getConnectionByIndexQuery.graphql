query getConnectionByIndex($index: Int!) {
  connection: getConnectionByIndex(index: $index) {
    id
    index
    cloud {
      id
    }
    dashboard
    name
    username
    endpoint
    token
    isStardogCloud
    isStardogFree
    isAllocating
    useBrowserAuth
    useSSO
  }
}
