mutation createDesignerProject(
  $name: String!
  $content: Base64Bytes!
  $connection_id: String
) {
  createDesignerProject(
    name: $name
    content: $content
    connection_id: $connection_id
  )
}
