query getDesignerProjects {
  getDesignerProjects {
    id
    name
    connection_id
    org_domain
    created_at
    updated_at
    owner {
      id
      username
    }
    roles {
      role
      user {
        id
        username
      }
    }
  }
}
